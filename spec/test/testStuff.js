// testStuff.js - test file for smth. (Capuccino)

describe('this is a test', () => {
    it('this is a task', () => {
        const nya = 'nya';
        console.log(nya);
        expect(nya).toBe(typeof nya === 'string');
    })
}) 